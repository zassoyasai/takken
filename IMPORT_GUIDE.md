# お手持ちの問題集をアプリに取り込むガイド

お手持ちの一問一答テキスト（スキャンPDFなど）を、ご自身の手でアプリに取り込むための手順です。

> **はじめに（大切な注意）**
> 市販教材の問題・解説は出版社に著作権があります。取り込んだデータはアプリを使う**ご自身の端末の中にだけ**保存され、ご自身の学習に使う分には私的利用の範囲ですが、
> **取り込んだデータやバックアップJSONを他人に渡したり、インターネット上（公開リポジトリ・SNS等）にアップロードすることは絶対にしないでください。**

## 全体の流れ

```
スキャンPDF → ① 文字起こし(OCR) → ② テキストを整形 → ③ アプリに貼り付け → ④ バックアップ
```

1,000問を一度にやる必要はありません。「今週使う分野の100問だけ」など、少しずつ取り込むのがおすすめです。

---

## ① PDFを文字データにする（OCR）

**まず確認**：PCやスマホでPDFを開き、問題文を指やマウスでなぞって**文字が選択・コピーできるか**試してください。
コピーできる場合、すでに文字データが入っているのでOCRは不要です。→ ②へ

コピーできない（画像だけの）場合は、無料でできるGoogleドライブのOCRが手軽です。

1. PDFを**20〜30ページ程度に分割**する（大きいままだとOCRが失敗しやすいため）
   - Windows: 「CubePDF Utility」（無料）でページを選んで保存
   - Mac: 「プレビュー」でページを選んでPDFとして書き出し
   - スマホのみの場合: PDF分割系の無料アプリ
2. 分割したPDFをGoogleドライブにアップロード
3. ファイルを右クリック →「アプリで開く」→「**Googleドキュメント**」
4. 文字起こしされたテキストが開くので、全選択してコピー

認識精度が悪い場合は、元のスキャンをもう少し高い解像度で取り直すと改善します。

## ② テキストをアプリ用の形式に整える

アプリは次の形式を読み取ります（1問＝「Q.の行」＋「A.の行」）：

```
Q. 宅地建物取引業の免許の有効期間は5年である。
A. ○ 免許の有効期間は5年で、更新申請は満了の90日前から30日前までに行う。
Q. （次の問題文…）
A. × （解説…）
```

- `Q.` から次の `Q.` までが1問。問題文・解説は複数行になっていても大丈夫です
- `A.` の直後の `○` または `×` が答えとして読み取られます。解説は省略可（`A. ○` だけでもOK）
- **「問1」「問2」のような行もQ.と同じ扱いで認識されます**。テキストがもともと「問1 〜」の形なら、Q.への置換は不要です
- 整形にはGoogleドキュメントの「編集」→「検索と置換」が便利です（例：「解説」→「A.」に一括置換）

OCR特有の誤字（「○」が「0」になる等）は、答えの行だけ目視で直せば十分です。問題文の細かい誤字は学習しながら気づいたときに直す、くらいの気持ちで大丈夫です。

## ③ アプリに貼り付ける

1. アプリの **設定 → 問題の追加（インポート）** を開く
2. **取り込む分野**（宅建業法／権利関係／法令上の制限／税・その他）を選ぶ
3. ②のテキストを貼り付けて「問題を追加する」

- **100〜200問ずつ**に分けて貼り付けるのがおすすめです
- 同じ問題文は自動的にスキップされるので、二重に貼っても重複しません
- 取り込んだ問題はすぐに「新規問題」として出題対象になります

JSON形式（`[{"q":"問題文","a":true,"exp":"解説","cat":"宅建業法"}]`）でも取り込めます。この場合は問題ごとに分野を指定できます。

## ④ バックアップを取る

取り込みが終わったら **設定 → バックアップ書出し** でJSONファイルを保存してください。
機種変更やブラウザのデータ削除時は「読込み」で復元できます。バックアップファイルにも教材の内容が含まれるため、取り扱いには注意してください。

---

## 画像のままカード化する（OCR不要・おすすめ）

文字起こしをせず、**問題と解答を画像のまま**フラッシュカードにする方法です。Ankiの画像カードと同じ使い方で、
「答えを見る」→「もう一度／難しい／普通／簡単」の自己評価で分散学習に乗ります。誤字の心配がないのが利点です。

### 方法A：アプリ内でページ画像から切り出す（一番ラク）

1. 教材のページ全体の画像を用意する
   - スマホならPDFを表示してスクリーンショット
   - PCならPDFの各ページを画像として書き出し（多くのPDFアプリで可能）
2. アプリの **設定 → 画像カード → 「ページ画像から切り出して追加」** でそのページ画像を開く
3. **問題の部分を指（マウス）でなぞる** → 「問題として保存」
4. **解答・解説の部分をなぞる** → 「解答として保存」でカード完成
5. 同じページの次の問題を続けてなぞる。ページが終わったら「別のページ画像を開く」

1ページに4問あるなら、1枚のスクショから4カード作れます。慣れると1問10秒程度です。

### 方法B：ZIPファイルから一括追加

「問題画像→解答画像」の順に交互に並ぶ画像ファイル群をZIPにまとめてあれば、
**設定 → 画像カード → 「ZIPファイルから一括追加」** でZIPを1つ選ぶだけで数百枚を一括登録できます。
ファイル名順（数字は数値として比較）に2枚ずつ組になります。例：`k084-1_1Q.jpg`（問題）と `k084-1_2A.jpg`（解答）。

### 方法C：切り抜き済みのスクショをまとめて追加

スマホのスクショ＋標準の切り抜き機能で「問題」「解答」を交互に撮りためた場合は、
**設定 → 画像カード → 「切り出し済み画像をペアで追加」** で複数枚まとめて選択してください。
ファイル名（撮影順）で並べて、2枚ずつ「問題→解答」の組として取り込まれます。

### 画像カードの注意

- 画像は**この端末のブラウザ内（IndexedDB）にのみ**保存されます。サーバーには送信されません
- **バックアップ書出しには画像は含まれません**（現バージョンの制限）。ブラウザのデータを消すと画像カードは失われます
- 教材の画像を他人に送ったり公開の場にアップロードすることは著作権侵害になります。ご自身の学習だけに使ってください

## ランク情報（重要度）の取り込み

カードの内容ハッシュとランク（A/B/C）の対応表JSON（`{"v":1,"ranks":{"ハッシュ":"A",...}}`）があれば、
**設定 → 画像カード → 「ランク情報を取り込む（JSON）」** で全カードに重要度を一括設定できます。
設定後はホーム画面に「重要度」フィルタが表示され、Rank Aだけに絞った学習などができます。

- ランクは分野フィルタと併用できます（両方の条件を満たす問題が出題されます）
- ランクをすべて選択している場合は、ランク未設定のカードも出題対象に含まれます
- ランク情報は同期にも含まれ、他のデバイスへ自動で行き渡ります

## 暗記ゾーン（まとめ表の暗記カード）

教材の「暗記」まとめ表を1枚1カードで取り込む専用モードです。通常の一問一答とは別枠で出題されます。

1. **設定 → 画像カード → 「暗記まとめZIPから追加（1枚=1カード）」** でZIPを選ぶ
   - ファイル名が `anki-分野-連番.jpg`（分野=gyo/ken/hor/zei）の形式なら分野は自動判定されます
2. ホームに **🧠 暗記ゾーン** が現れます。「暗記する」で開始
3. 出題は「ぼかし画像を見る → 中身を思い出す → 表示して確認 → 自己評価」の流れで、FSRSの復習スケジュールに乗ります
4. 1日に新しく出す枚数は 設定 → 「1日の新規暗記カード」で調整できます（通常の新規問題数とは別枠）

暗記カードも同期・削除伝搬・画像復元（再ZIP取り込み）の対象です。画像は端末内にのみ保存されます。

## 機種変更・別ブラウザへの移行（画像カードの復元）

学習データは端末のブラウザごとに保存されるため、機種変更やアプリのURL変更（Artifact版→GitHub Pages版など）のときは次の手順で移行します。

1. **旧環境**：設定 → バックアップ書出し でJSONファイルを保存
2. **新環境**：アプリを開き、設定 → 読込み でそのJSONを読み込む（学習履歴・カード情報が戻ります）
3. **新環境**：各分野のZIPを「ZIPファイルから一括追加」で再度取り込む
   - 取り込み時に「◯枚の画像を復元」と表示され、既存の学習履歴に画像が再接続されます
   - カードが増えたり履歴が消えたりすることはありません

## デバイス間の同期（スマホ・PC・タブレット）

GitHubの非公開Gistを使って、学習記録を複数デバイスで同期できます（GitHub Pages版で利用してください）。

**初回設定（1回だけ）**

1. GitHubにログイン → 右上アイコン → Settings → 最下部 **Developer settings** → **Personal access tokens** → **Tokens (classic)**
2. **Generate new token (classic)** → Note欄に「takken sync」等 → Expiration は1年など →
   スコープは **gist だけ**にチェック → Generate token
3. 表示された `ghp_…` をコピー（この画面でしか見られません）
4. 各デバイスのアプリで：設定 → **デバイス間同期** → トークンを貼り付けて「接続」

**動作**

- アプリを開いたとき・学習セッションを終えたときに自動同期します（「今すぐ同期」で手動も可）
- カードごとに「最後に学習したデバイスの記録」が採用されます
- 同期されるのは学習記録・進捗のみで、**教材の画像はアップロードされません**。新しいデバイスでは初回にZIPを取り込むと画像が復元されます
- トークンは端末内にのみ保存されます。gist権限しか無いため、万一漏れてもリポジトリ等は操作できませんが、他人に教えないでください

## よくある質問

**Q. 収録済みの問題（210問）と混ざりますか？**
混ざって出題されます。設定の分野チップで絞り込みも可能です。不要なら今後「収録問題を非表示にする」機能も追加できます。

**Q. 取り込んだ問題を修正・削除したい**
画像カードは、学習画面の右上の🗑ボタンでその場で削除できます。誤って二重に取り込んだ場合は、設定→画像カード→「重複カードを削除」で自動整理できます（同じ内容の取り込みは自動でスキップもされます）。テキスト問題の修正は、バックアップJSONを書き出して編集し、読込みで戻すのが確実です。

**Q. データはどこに保存されていますか？**
お使いのブラウザ（端末）内のローカルストレージにのみ保存されます。サーバーには送信されません。
